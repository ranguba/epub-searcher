- if @drilldowns
  %ul.author-drilldowns
    - @drilldowns.first.items.each do |author_drilldown|
      %a{:href => "?q=#{[@query_words, author_drilldown['_key']].join('+')}"}
        %li
          #{author_drilldown['_key']}
          %span= author_drilldown['_nsubrecs']
